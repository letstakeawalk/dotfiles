- Create a new worktree on bare repo

`git worktree add {{path/to/directory}} -b {{branch_name}} {{base_branch}}`

- Create a new throwaway worktree with detached HEAD

`git worktree add -d {{path/to/directory}}`
